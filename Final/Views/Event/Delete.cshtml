@model Final.Models.Event

<h2>Delete</h2>

<h3>Are you sure you want to delete: @Model.EventName</h3>

@using (Html.BeginForm("ConfirmDelete", "Event", FormMethod.Post)) {
	<input type="hidden" asp-for="EventId" />
	<button asp-action="Index">Back</button>
	<button class="btn btn-danger" type="submit">Delete</button>
}
